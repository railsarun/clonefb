<center>

<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @post.name %>
</p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @post.description %>
</p>

<p>
  <strong>Age:</strong>
  <%= @post.age %>
</p>

<p>
  <strong>Address:</strong>
  <%= @post.address %>
</p>


  <p>
  <strong>Time:</strong>
  <%= @post.update_at%>
</p>

<p>
  <strong>Image:</strong> 
  <td><%= image_tag @post.image ,:size => '360x250'%></td>
        <td>
  <%#= image_tag @post.image %>
</p>
               <p>
                <strong>Comment:</strong>
                <%# @post.comments.each do |comment|%>
                <%#= comment.content%><br>
                <% #end %>
                <%= render @post.comments%>
                <%=render partial: 'comments/form',locals:{post:@post} %>
              </p>
   
              <p>
                <% if current_user.id == @post.user_id %>
                <td><%= link_to 'Edit', edit_post_path(@post) ,class: "btn btn-secondary"%></td>  
               <%end%>

              </p>

<%#= link_to 'Edit', edit_post_path(@post) %> |

        <% if current_user.id == @post.user_id %>
        <th><%= link_to 'Destroy', @post, method: :delete, data: { confirm: 'Are you sure?' },class: "btn btn-danger text-white"%></th>
        <%end%>
         <%= link_to 'Save and Share', posts_path,class: "btn btn-success text-white" %>
<%#= link_to 'Destroy', post_path, method: :delete, data: { confirm: 'Are you sure?' } %>
<center>